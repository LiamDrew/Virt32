#include "recycler.h"
#include <stdlib.h>

uint32_t find_freed_segment(Mem_T *mem, uint32_t size)
{
    (void)mem;
    (void)size;
    return 0;
}

void free_segment(Mem_T *mem, uint32_t seg_addr)
{
    (void)mem;
    (void)seg_addr;
    return;
}

